# Policy Schema Contract Changelog

## v1 revision 1

- Established the versioned contract for the existing YAML policy root and
  policy properties.
- Defined required fields, scalar and plural targets, decision values, exact
  types, and rejection of unknown properties.
- Distinguished schema validation from semantic and referential validation.
